# Sublima Decryptor v0.3.0 — Estabilidade

**Data código:** 2026-08-20  
**Validação testes:** 2026-08-29 (5/5 passed)  
**Ordem no portfólio:** 1 (após Hub / Ordem 0)  
**Marca:** © DcsProducer®

## Definição de pronto

| Critério | Status |
|----------|--------|
| Health estável | ✅ `/api/health` |
| Analyze estável | ✅ validação upload + relatório |
| Relatório em camadas + disclaimer | ✅ |
| LSB no pipeline | ✅ `layers.steganography` |
| Testes mínimos | ✅ `backend/tests/test_api.py` — 5 passed |
| Tag `v0.3.0` | ⏳ operador no GitHub |
| Redeploy Render | ⏳ confirmar health `version: "0.3.0"` |

## Como testar local

```bash
cd backend
pip install -r requirements.txt
pytest -q
uvicorn app.main:app --reload --port 8000
# POST /api/analyze com uma imagem
```

## Produção

Live: https://sublima-decryptor.onrender.com/docs  
Após redeploy, health deve retornar:

```json
{
  "status": "ok",
  "version": "0.3.0",
  "phase": "v0.3-stable",
  "stego_lsb": true,
  "ocr_available": true
}
```

### Criar tag (operador)

```bash
git tag -a v0.3.0 -m "v0.3.0 — estabilidade + LSB + testes"
git push origin v0.3.0
```

Ou no GitHub: Releases → Create a new release → tag `v0.3.0` no commit `f933abc` (ou HEAD após este doc).

© DcsProducer® Creative Studio
