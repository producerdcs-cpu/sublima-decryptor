# OCR — Fase 1.2

Tesseract (`por+eng`) via `backend/app/services/ocr.py`.

Integrado no relatório: `layers.ocr` com texto, word_count, confidence e possível microtexto.

Docker inclui `tesseract-ocr-por` e `tesseract-ocr-eng`.
